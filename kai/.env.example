# Server-side only. NEVER prefix with VITE_ — Vite would bundle it
# into the client. Set this in Vercel: Project → Settings → Environment
# Variables. The /api/claude Edge function reads it.
ANTHROPIC_API_KEY=sk-ant-...
